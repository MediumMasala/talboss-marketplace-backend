-- Paste into Supabase SQL editor → Run.
-- Marketplace now excludes tier1 + supreme (they're handled separately).
-- Marketplace pool = tier2 candidates + medium-confidence maybes only.

create or replace function daily_breakdown(start_date date, end_date date)
returns table (
  joined_at date,
  total bigint,
  marketplace bigint,
  tier1_supreme bigint,
  tal_users bigint,
  round1 bigint
)
language sql
stable
as $$
  select
    joined_at,
    count(*) as total,
    count(*) filter (
      where (is_marketplace = true and confidence = 'high' and tier = 'tier2')
         or confidence = 'medium'
    ) as marketplace,
    count(*) filter (
      where is_marketplace = true
        and tier in ('tier1','supreme')
    ) as tier1_supreme,
    count(*) filter (where source_table in ('tal_users','both')) as tal_users,
    count(*) filter (where source_table in ('round1_god_table','both')) as round1
  from candidates_daily
  where joined_at between start_date and end_date
  group by joined_at
  order by joined_at;
$$;
